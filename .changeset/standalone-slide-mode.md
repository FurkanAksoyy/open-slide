---
"@open-slide/core": minor
"@open-slide/cli": minor
---

Add a standalone single-slide mode (`mode: 'standalone'`) that runs a root-level `index.tsx` with no slide browser, folders, or themes — selectable in `open-slide init` via a prompt or `--standalone`.
